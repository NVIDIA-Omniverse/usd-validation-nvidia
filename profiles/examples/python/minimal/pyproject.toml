[project]
name = "usd-profiles-minimal-example"
version = "0.1.0"
description = "Minimal usd-profiles-nvidia code generation example"
requires-python = ">=3.10,<3.15"
dependencies = [
    "usd-profiles-nvidia",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
